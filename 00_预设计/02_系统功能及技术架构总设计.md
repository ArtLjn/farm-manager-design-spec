# 02 — 系统功能及技术架构总设计

> 状态：草稿 | 维护：BlockShip | 关联：[01_系统愿景与目标](./01_系统愿景与目标.md)、[03_技术选型与依赖](./03_技术选型与依赖.md)

---

## 1. 系统总览

Farm Manager 是一个三层架构的 AI 农场管理平台：

```
┌──────────────────────────────────────────────────────────────────┐
│                          客户端层                                 │
│                                                                  │
│   ┌────────────────┐  ┌────────────────┐  ┌──────────────────┐  │
│   │  Flutter App   │  │   Admin Web    │  │  Playground/Web  │  │
│   │  (农户主入口)   │  │  (管理/调试)    │  │  (联调/调试)      │  │
│   └────────┬───────┘  └────────┬───────┘  └────────┬─────────┘  │
└────────────┼───────────────────┼────────────────────┼────────────┘
             │ REST + SSE         │ REST                │ REST + SSE
             ▼                    ▼                     ▼
┌──────────────────────────────────────────────────────────────────┐
│                        API Gateway (FastAPI)                     │
│                                                                  │
│   auth | farm | crop | cycle | cost | log | weather | feedback   │
│   agent (chat / stream / advice / report) | smart_fill | admin   │
│                                                                  │
│   ┌──────────────────────────────────────────────────────────┐   │
│   │             Auth Middleware（JWT + Farm 隔离）            │   │
│   └──────────────────────────────────────────────────────────┘   │
└────────────────────────────┬─────────────────────────────────────┘
                             │
┌────────────────────────────▼─────────────────────────────────────┐
│                     Agent 平台层（核心）                          │
│                                                                  │
│  ┌─────────────────┐  ┌─────────────────┐  ┌──────────────────┐  │
│  │ Application     │  │ Advisor         │  │ Runtime          │  │
│  │ (UseCase 编排)   │→│ (Guardrails/    │→│ (LangGraph 节点)  │  │
│  │                 │  │  Pending/问候)  │  │                  │  │
│  └─────────────────┘  └─────────────────┘  └────────┬─────────┘  │
│                                                      │            │
│  ┌─────────────────┐  ┌─────────────────┐  ┌────────▼─────────┐  │
│  │ Planner         │  │ Executor        │  │ Reflector        │  │
│  │ (意图/工具候选)  │  │ (并行 Skill)    │←│ (写操作/一致性)   │  │
│  └────────┬────────┘  └────────┬────────┘  └──────────────────┘  │
│           │                    │                                   │
│           ▼                    ▼                                   │
│  ┌──────────────────────────────────────────────────────────────┐│
│  │  Prompt │ Context │ Memory │ Skills（30+ 业务能力）            ││
│  │  工程   │ 工程    │ 工程   │                                  ││
│  └──────────────────────────────────────────────────────────────┘│
└────────────────────────────┬─────────────────────────────────────┘
                             │
┌────────────────────────────▼─────────────────────────────────────┐
│                  数据飞轮 / 评测 / 可观测                         │
│                                                                  │
│   Discovery Layer | DataFlywheel | Simulation | Evaluation       │
│   TraceMonitor | Token Stats | Prompt Replay | Skill Coverage    │
└────────────────────────────┬─────────────────────────────────────┘
                             │
┌────────────────────────────▼─────────────────────────────────────┐
│             业务模块（modules/）+ 服务（services/）              │
│                                                                  │
│   auth | farm | crop | cycle | ledger | weather | conversation  │
│   feedback | admin | report | quota | labor                      │
└────────────────────────────┬─────────────────────────────────────┘
                             │
┌────────────────────────────▼─────────────────────────────────────┐
│              基础设施（infra / core）+ 数据（MySQL 8.x）          │
│                                                                  │
│   trace | cache | limiter | circuit_breaker | pending_action     │
│   config | database | logger | security | llm_client_manager     │
│                                                                  │
│   farms | crops | cycles | costs | logs | conversations | ...    │
└────────────────────────────┬─────────────────────────────────────┘
                             │
                             ▼
              外部服务：LLM Provider | QWeather | Open-Meteo
```

## 2. 五大子系统

| 子系统 | 职责 | 主目录 | 主文档 |
| --- | --- | --- | --- |
| **API Gateway** | HTTP/SSE 入口、认证、参数校验、Farm 隔离 | `app/api/`, `app/bootstrap/`, `app/modules/auth/`, `app/modules/farm/` | [03.01 HTTP API](../03_接口协议/01_HTTP_API协议.md) |
| **Agent 平台** | 意图识别、上下文构建、Prompt 渲染、Skill 调用、反思、流式回复 | `app/agent/`, `app/prompt/`, `app/context/`, `app/memory/` | [01.01 Agent 平台](../01_正式设计/01_Agent平台架构.md) |
| **Skill 引擎** | Skill 注册、权限、参数校验、写操作确认、并行执行 | `app/agent/skills/`, `app/agent/executor/` | [01.02 Skill 引擎](../01_正式设计/02_Skill引擎与契约.md) |
| **数据飞轮** | Discovery Layer（风险发现）、样本治理、AI 预标注、回归 case、评测报告、训练出口 | `app/evaluation/`, `app/simulation/`, `app/observability/` | [01.06 数据飞轮与评测](../01_正式设计/06_数据飞轮与评测.md) |
| **业务模块** | 农场、作物、茬口、记账、天气、日志、工人、债务 | `app/modules/`, `app/services/`, `app/models/` | [01.08 业务模块化](../01_正式设计/08_业务模块化.md) |

## 3. 标准请求生命周期

以「农户说：上个月买了 200 块化肥，老王农资店赊账」为例：

```
1. Flutter App 输入 → POST /api/v1/agent/chat/stream
2. bootstrap middleware：JWT 校验 → 解析 farm_id → 注入 trace_id
3. api/agent.py 转发到 agent/application/chat_use_case
4. chat_use_case：
   ├─ 调用 conversation_service 保存用户消息
   ├─ 调用 pending_actions 检查是否有待确认动作
   └─ 调用 advisor.invoke_advisor / stream_advisor
5. advisor：
   ├─ check_input（Guardrails）
   ├─ classify_intent（问候/记账/查询/闲聊）
   └─ 编译并调用 LangGraph 图
6. runtime/graph_factory：
   ├─ nodes.agent_node 准备 LLM 调用
   ├─ planner.intent + tool_selector 给出候选 Skill
   ├─ context.builder 构造 ContextBundle（farm/cycle/ledger/memory）
   ├─ prompt.composer 渲染 system prompt + skill 摘要
   └─ 调用 LLM 拿到 tool_calls
7. runtime.tool_executor：
   ├─ 解析 tool_calls（如 create_cost_record）
   ├─ 校验权限（write_confirm?）
   ├─ 若是写操作 → 入 pending_actions，返回确认话术
   └─ Reflector 检查 tool_call 与回复一致性
8. response.formatter：
   ├─ 普通回复：直接 JSON
   └─ 流式回复：SSE 事件（token / tool_call / pending / done）
9. application 提交 Memory observation event
10. observability 提交 trace + token_stats
11. conversation_service 保存 assistant 消息
12. Flutter App 渲染：「已记账：化肥 200 元，向老王农资店赊账，确认？」
13. 用户点确认 → /api/v1/pending/{id}/confirm → 真正写入
```

## 4. 横切关注点

| 关注点 | 实现位置 | 接入方式 |
| --- | --- | --- |
| **认证** | `modules/auth/dependencies.py` | FastAPI Depends 注入 `current_user` |
| **Farm 隔离** | `modules/farm/dependencies.py` | Depends 注入 `current_farm` |
| **Trace** | `infra/trace_context.py`, `infra/trace_collector.py` | ContextVar + decorator |
| **限流** | `infra/limiter.py` | Decorator on API |
| **缓存** | `infra/cache.py`, `prompt/cache.py`, `context/cache.py` | 各自专用，不混用 |
| **熔断** | `infra/circuit_breaker.py` | LLM Provider 调用前 |
| **日志** | `core/logger.py` | `logging.getLogger(__name__)` |
| **配置** | `core/config.py` | Pydantic Settings + `.env` |
| **数据库** | `core/database.py` | SQLAlchemy SessionLocal |
| **LLM 客户端** | `core/llm_client_manager.py` | Provider 路由 + 多模型 fallback |

## 5. 关键设计决策

### 5.1 为什么 Agent 平台是核心，不是业务模块？

业务模块（crop/cycle/ledger）逻辑稳定且简单，**真正难的是让 Agent 正确理解农户意图、选择正确 Skill、构造合适上下文、生成可信回复**。Agent 平台的工程化深度直接决定产品上限。

### 5.2 为什么用 LangGraph 而不是裸 Anthropic SDK？

- 多步骤任务需要状态机（记账 → 确认 → 写入 → 反馈）
- 流式回复需要 SSE 事件流（token / tool_call / pending / done）
- 调试需要图可视化（trace + replay）
- 业内成熟，社区活跃

不选 LangChain Agent 是因为它的 message 流转黑盒，无法在 2s SLA 下精准调优；不用裸 SDK 是因为状态机、流式、回放都要自己写，性价比低。

### 5.3 为什么 Skill 必须有 `skill.md`？

- LLM 需要结构化元数据来决定何时调用（trigger / parameters / 不要使用）
- 工程需要单一契约来注册、校验、生成 API 文档
- 测试需要 fixture
- 文档与代码强制对齐，避免「实现改了文档没改」

详见 `.claude/rules/skill-writing.md`。

### 5.4 为什么 Memory 暂时不接外部 RAG？

2 核 4G 部署无法负担 embedding 模型 + 向量库 + 重排。当前 `MemoryService.build_context()` 返回空长期记忆，`MemoryService.search()` 返回空检索；Agent 靠短时记忆 + 业务工具 + Prompt 正常执行。**预留端口**，等部署升级或外接 RAG 服务时再接入。

### 5.5 为什么写操作必须 Pending？

农业场景写错的代价高（误删账、误记账、误改茬口）。所有写 Skill 默认 `direct_call: false`，必须经过 Pending Action 二次确认。Reflector 还会检查「回复里说执行了但 tool_call 没成功」的幻觉执行。

### 5.6 为什么 DataFlywheel 是一等公民？

不是事后补丁，而是从第一天就埋的护城河。真实会话比评测集宝贵 10 倍，且免费。飞轮闭环让产品自我进化，是中小团队对抗大厂的唯一路径。

### 5.7 为什么 DataFlywheel 必须有 Discovery Layer？

农场场景下 **95%+ 的真实会话是正常会话**（"你好"、"今天天气怎么样"、"你是谁"）。让标注员从全量会话中人工捞坏例，效率极低、覆盖极差、反馈延迟 > 3 天。

Discovery Layer 通过 **规则命中 + LLM Judge** 两级信号计算 `risk_score`，默认按风险倒序推送，自动隐藏低风险会话。让标注员只看值得标注的样本，这是数据飞轮从「能跑」到「能转」的关键升级。设计原则：不引入新基础设施（复用 Postgres + cron + 现有 LLM client）、不学权重（1000+ 标注前手工权重足够）、Judge 不作为最终真值。

详见 [01.06 数据飞轮与评测 § 9 Discovery Layer](../01_正式设计/06_数据飞轮与评测.md)。

## 6. 与典型 Agent 架构的差异

| 维度 | 典型 Agent 架构 | Farm Manager |
| --- | --- | --- |
| 客户端 | 智能音箱（语音 + WebSocket） | Flutter App + Admin Web（文本 + REST/SSE） |
| 任务复杂度 | 设备控制（短任务、明确意图） | 记账/茬口/工资（长任务、模糊意图、跨茬口） |
| Agent 形态 | Master + SubAgent（设备控制/场景创建） | 单一 Master + 30+ Skill，无 SubAgent |
| 快慢思维 | 强需求（<500ms 设备控制） | 弱需求（记账延迟 1-3s 可接受，但每日建议要快） |
| 记忆形态 | 设备别名/用户习惯（短周期） | 农场历史/茬口/赊账（跨年周期） |
| 数据飞轮 | 未提及 | 一等公民，从 P0 起 |
| 部署形态 | 大厂集群 | 2C4G 单机 |

## 7. 当前落地状态

详见 [06_项目管理/01_里程碑与路线图](../06_项目管理/01_里程碑与路线图.md)。简版：

| 模块 | 状态 |
| --- | --- |
| API Gateway | ✅ 已落地（含 auth、farm、JWT） |
| Agent 平台 Application/Runtime/Planner/Executor | ✅ 已落地 |
| Advisor 兼容入口 + Guardrails + Pending | ✅ 已落地 |
| Reflector | ✅ 已落地 |
| Prompt/Context/Memory 工程 | 🚧 骨架已建，治理细化中 |
| Skill 引擎 + 30+ Skill | ✅ 已落地，持续扩展 |
| DataFlywheel | 🚧 列表/详情/标注/case draft 已落地，规则候选/AI 预标注/仿真闭环中 |
| Discovery Layer（风险发现） | 🚧 待落地：rule engine + judge worker + 工作台排序改造（MVP 4.5 人日，详见 01.06 § 9） |
| 业务模块化（modules/） | 🚧 auth、farm 已迁移；crop/cycle/ledger 待迁 |
| 移动端 Flutter App | ✅ 已落地（首页/记账/茬口/工人/AI 对话/工作台） |
| Admin Web | ✅ 已落地（Dashboard/Skill 注册表/Trace/DataFlywheel/Prompt Inspector） |

## 8. 相关文档

- [01_系统愿景与目标](./01_系统愿景与目标.md)
- [03_技术选型与依赖](./03_技术选型与依赖.md)
- [01_正式设计/01_Agent平台架构](../01_正式设计/01_Agent平台架构.md)
- [03_接口协议/01_HTTP_API协议](../03_接口协议/01_HTTP_API协议.md)
- 现有：[../../docs/architecture/overview.md](../../docs/architecture/overview.md)、[../../docs/architecture/backend-architecture.md](../../docs/architecture/backend-architecture.md)
